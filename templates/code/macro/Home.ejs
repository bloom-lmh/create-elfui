import { defineHtml, html<% if (hasStyle) { %>, defineStyle<% } %> } from "@elfui/core";
<% if (hasStyle) { -%>
import styles from "./page.<%= styleExtension %>?inline";

defineStyle(styles);
<% } -%>

export default defineHtml(html`
  <section class="page home-page">
    <div class="hero">
      <div class="hero-copy">
        <p class="eyebrow">WEB COMPONENT FRAMEWORK</p>
        <h1>ElfUI</h1>
        <p class="hero-description">一款专为组件而生的 Web Component 框架</p>
        <elf-link class="hero-link" to="/example">查看组件示例 <span aria-hidden="true">→</span></elf-link>
      </div>
      <div class="hero-mark-wrap" aria-label="ElfUI 标识">
        <img class="hero-mark" :src=${new URL("../assets/elfui-mark.png", import.meta.url).href} alt="ElfUI 雪花标识" />
      </div>
    </div>
  </section>
`);
