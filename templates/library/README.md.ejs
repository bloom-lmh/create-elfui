# <%= packageName %>

An ElfUI component library built with <%= componentMode %> components.

```bash
<%= packageManager %> install
<%= packageManager === "npm" ? "npm run build" : `${packageManager} build` %>
```
