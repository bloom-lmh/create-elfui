import { describe, expect, it } from "vitest";

import { ElfLibraryButton } from "../index";

describe("ElfLibraryButton", () => {
  it("exports the library component", () => {
    expect(ElfLibraryButton).toBeDefined();
  });
});
