import { defineHtml<% if (hasStyle) { %>, defineStyle<% } %> } from "<%= macroImport %>";
<% if (hasStyle) { -%>
import styles from "./<%= componentName %>.<%= styleExtension %>?inline";

defineStyle(styles);
<% } -%>

export default defineHtml(`
  <section class="<%= kebabName %>">
    <slot></slot>
  </section>
`);
