import { defineConfig } from "vitest/config";
<% if (componentMode === "macro") { -%>
import { elfuiMacroPlugin } from "@elfui/vite-plugin";
<% } -%>

export default defineConfig({
<% if (componentMode === "macro") { -%>
  plugins: [
    elfuiMacroPlugin({
      macroImport: "@elfui/core",
      runtimeImport: "@elfui/core"
    })
  ],
<% } -%>
  test: {
    environment: "jsdom"
  }
});
