import { expect, test } from "@playwright/test";

test("renders the ElfUI application", async ({ page }) => {
  await page.goto("/");

  await expect(page.locator("body")).toContainText("ElfUI");
});
