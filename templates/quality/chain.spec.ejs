import { describe, expect, it } from "vitest";

import App from "../App";

describe("App", () => {
  it("registers the root custom element", () => {
    expect(customElements.get("elf-app")).toBe(App);
  });
});
