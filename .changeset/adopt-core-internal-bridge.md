---
"create-elfui": patch
---

Generate Macro applications and component libraries for the ElfUI beta.6 Core internal bridge. New scaffolds use `@elfui/core` as their only direct runtime dependency, externalize `@elfui/core/internal` in component libraries, and follow the independent Router and Chain beta versions.
