---
"create-elfui": patch
---

Pin generated Macro projects to matching `@elfui/core` and `@elfui/vite-plugin` beta.13 releases, reject ranged or mismatched framework versions, and verify Vite reports version mismatches during startup.
