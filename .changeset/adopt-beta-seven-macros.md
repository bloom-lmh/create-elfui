---
"create-elfui": patch
---

Upgrade generated Macro projects to ElfUI beta.7 and replace the removed `html`/`css` tagged-template helpers with direct string arguments in every application, library, and component-generator template.
